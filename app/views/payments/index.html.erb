<section>
  <h2>Payments</h2>
  <p>Total price: $<%= @category.total_price %></p>
  <% unless @payments.empty? %>
    <%= render 'list' %>
  <% else %>
    <p>No payments available</p>
  <% end %>
  <%= link_to 'Add a new payment', new_payment_path %>
</section>