<section>
  <h1>Categories</h1>
  <% unless @categories.empty? %>
    <%= render 'list' %>
  <% else %>
    <p>No categories available</p>
  <% end %>
  <%= link_to 'Add a new category', new_category_path %>
</section>